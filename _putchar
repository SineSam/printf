#include <stdio.h>
#include "main.h"
	#include <unistd.h>
/**
 * _putchar - writes character c to stdout
 * @c:The character to print
 *
 * Return: to 1 (Success)
 * on error, Return -1, erno set appropriately
 **/
int _putchar(char c)
{
	return (write(1, &c, 1));
}
